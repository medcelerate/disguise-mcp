# Binaries and build output
/disguise-mcp
/dist/
/build/
*.exe
*.mcpb

# Downloaded build tools (Tailwind CLI, etc.)
/.tools/

# Local config (do not commit real deployment config)
/config.yaml

# Editor / OS
.DS_Store
*.swp
.idea/
.vscode/
